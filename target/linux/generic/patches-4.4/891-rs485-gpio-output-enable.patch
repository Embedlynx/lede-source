--- a/include/uapi/linux/serial.h
+++ b/include/uapi/linux/serial.h
@@ -123,8 +123,12 @@ struct serial_rs485 {
 #define SER_RS485_RX_DURING_TX		(1 << 4)
 	__u32	delay_rts_before_send;	/* Delay before send (milliseconds) */
 	__u32	delay_rts_after_send;	/* Delay after send (milliseconds) */
-	__u32	padding[5];		/* Memory is cheap, new structs
-					   are a royal PITA .. */
+#define SER_RS485_USE_GPIO          (1 << 5)
+    __u32   gpio_pin;               /* OutputEnable gpio pin number */
+    __u32   delay_oe_before_send;   /* Pull-up OutputEnable pin n miliseconds before send */
+    __u32   delay_oe_after_send;    /* Pull-down OutputEnable pin n miliseconds after send */
+    __u32   padding[2];     /* Memory is cheap, new structs
+                       are a royal PITA .. */
 };
 
 #endif /* _UAPI_LINUX_SERIAL_H */
